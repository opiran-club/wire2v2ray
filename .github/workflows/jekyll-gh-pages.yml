# Workflow for building and deploying a Jekyll site to GitHub Pagesname:DeployJekyllwithGitHubPagesdependenciespreinstalledon:# Trigger this workflow on push to the main branch and manually via the Actions tabpush:branches: ["main"]

  workflow_dispatch:permissions:contents:read# Required to read repository contentspages:write# Required to deploy to GitHub Pagesid-token:write# Required for deployment tokenconcurrency:group:"pages"cancel-in-progress:false# Do not cancel in-progress runsjobs:# Job to build the sitebuild:runs-on:ubuntu-lateststeps:-name:Checkoutrepositoryuses:actions/checkout@v4with:fetch-depth:0# Fetch all history for Jekyll build-name:SetupPagesuses:actions/configure-pages@v5-name:BuildtheJekyllsiteuses:actions/jekyll-build-pages@v1with:source:./destination:./_site-name:Uploadbuildartifactuses:actions/upload-pages-artifact@v3with:path:_site# This is the local path of the built site directory# Job to deploy the built sitedeploy:runs-on:ubuntu-latestneeds:buildenvironment:name:github-pagesurl:${{steps.deployment.outputs.page_url}}steps:-name:DeploytoGitHubPagesid:deploymentuses:actions/deploy-pages@v4
